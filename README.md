# xguardi dotfiles (config files)

## Installation

Use the stow to create the corresponding symlinks at the right locations:

$ stow nvim tmux zsh starship wezterm

or run them one by one:

$ stow zsh
etc.
